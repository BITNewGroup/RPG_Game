

function newStats(health, defence, attack)
{
	var stats = {
		attack : attack,
		health : health,
		defence : defence
	};
}

function atatck(statsAttacker, statsDefender)
{
	//Attacker stats
	var attAttack = statsAttacker;
	var attDefence = statsAttacker;
	var attHealth = statsAttacker;
	
	//Defender stats
	var defAttack = statsDefender;
	var defDefence = statsDefender;
	var defHealth = statsDefender;
	
	//F for attack
	
	
}